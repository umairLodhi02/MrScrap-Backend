const userController = require("./user");
const feedbackController = require("./feedback");
const scrapController = require("./scrap");
const complaincontroller = require("./complaints");

module.exports = {
  userController,
  feedbackController,
  scrapController,
  complaincontroller,
};
